// This binary is temporarily disabled until the schema conversion feature is fully implemented
// #![cfg(feature = "schema-convert")]

// use clap::{App, Arg};
// use std::path::PathBuf;
// use supabase_rust_postgrest::schema::generate_rust_from_typescript_cli;

fn main() {
    println!("Schema conversion is disabled in this version.");
    println!("This feature will be available in a future release.");
}
